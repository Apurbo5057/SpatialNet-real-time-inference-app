package com.example.roidetection.ui.theme

import androidx.compose.ui.graphics.Color

// The "beam" identity: light running from the fingertip to the thing being pointed at.

/** Deep night-navy: the camera glass and the main text colour on light screens. */
val Ink = Color(0xFF12233D)
val InkSoft = Color(0xFF2A3D5C)

/** Amber beam: answers, the pointing ray, and the one main action on each screen. */
val Beam = Color(0xFFFFB938)
val BeamDeep = Color(0xFF9A6200)   // amber that passes contrast on Paper

/** Cool light background for home and lists. */
val Paper = Color(0xFFF2F5F9)
val PaperRaised = Color(0xFFFFFFFF)

/** Secondary text on glass; hairlines. */
val Mist = Color(0xFFC3CFDF)
val Slate = Color(0xFF55657E)       // secondary text on Paper

val Coral = Color(0xFFFF7A66)

/** Camera glass: Ink at 72%, readable over any camera picture without hiding it. */
val Glass = Color(0xB812233D)
val GlassLine = Color(0x33FFFFFF)

// One muted tint per mode, used only on its icon tile, so people learn which is which.
val TintIdentify = Color(0xFFFFE2A8)
val TintObjects = Color(0xFFC8EBDD)
val TintRead = Color(0xFFD3E4FF)
val TintEarbuds = Color(0xFFE6DAFF)
